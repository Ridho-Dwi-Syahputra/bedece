# Referensi versi minimum (PRD §12). Di notebook (Colab/Kaggle), torch/torchvision/numpy/pandas/
# scikit-learn/scipy/pillow/tqdm dibiarkan memakai versi bawaan platform (sudah >= batas di bawah,
# dan dipakai puluhan paket bawaan lain) — hanya timm/imagehash yang benar-benar di-pin exact di
# tiap notebook karena tidak tersedia bawaan. Versi aktual yang dipakai selalu dicatat via
# pip freeze / print(__version__) ke dalam manifest & log tiap notebook untuk audit trail.
torch>=2.1
torchvision>=0.16
timm>=1.0
scikit-learn>=1.1
numpy>=1.26
pandas>=2.1
pillow>=10.0
imagehash>=4.3
scipy>=1.11
tqdm>=4.66
kaggle>=1.6
